{
  "version": "1.0.0",
  "name": "2if",
  "description": "A toy UI framework",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "tsc --project tsconfig.server.json && node server.cjs"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "tsx-dom-types": "^1.0.0"
  },
  "dependencies": {
    "@types/node": "^18.11.9",
    "tsx-dom": "^2.0.0"
  }
}
